name: trendpulse_app
description: TrendPulse Social Media Sentiment Analysis Dashboard
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # HTTP client
  http: ^1.1.0

  # State management
  provider: ^6.1.0

  # Charts
  fl_chart: ^0.65.0

  # Local storage
  shared_preferences: ^2.2.0

  # URL launcher
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
