# Reddit API Credentials
# Get from: https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=your_reddit_client_id_here
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here
REDDIT_USER_AGENT=trendpulse/1.0 (by /u/your_username)

# YouTube API Key
# Get from: https://console.cloud.google.com
YOUTUBE_API_KEY=your_youtube_api_key_here

# X/Twitter API (Optional - if using official API)
# Get from: https://developer.twitter.com/
TWITTER_BEARER_TOKEN=your_twitter_bearer_token_here

# LLM Provider Selection
# Options: 'openai' or 'tongyi' (default)
LLM_PROVIDER=tongyi

# OpenAI Configuration
# Sign up at: https://platform.openai.com/
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1

# Tongyi Qianwen Configuration (Alibaba)
# Sign up at: https://dashscope.aliyun.com/
TONGYI_API_KEY=your_tongyi_api_key_here
TONGYI_MODEL=qwen-plus
TONGYI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Legacy Configuration (for backward compatibility)
LLM_API_KEY=your_llm_api_key_here
LLM_API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen-plus

# Database
DATABASE_URL=sqlite+aiosqlite:///./trendpulse.db

# Settings
# Maximum posts to fetch per platform (conservative limit to avoid IP blocking)
MAX_POSTS_PER_PLATFORM=5
DEFAULT_LANGUAGE=en

# IMPORTANT: To avoid YouTube IP blocking:
# - Keep MAX_POSTS_PER_PLATFORM at 5 or lower
# - Use Webshare residential proxy (configured above)
# - Do not run scraping tasks too frequently (recommend every 30+ minutes)
# - Each request has 3-6 second random delays built-in


# Proxy Configuration (Required in China and for YouTube Transcript API)
# If you're in China or need proxy to access Google/YouTube APIs
#
# IMPORTANT: YouTube Transcript API may block IPs from cloud providers (AWS, GCP, Azure)
# If you encounter "IP blocked" or "RequestBlocked" errors, you MUST use a proxy.
#
# Option 1: Webshare Residential Proxy (Recommended for Production)
# Sign up at: https://www.webshare.io/?referral_code=w0xno53eb50g
# Purchase "Residential" proxy package (NOT "Proxy Server" or "Static Residential")
# Get credentials from: https://dashboard.webshare.io/proxy/settings
WEBSHARE_PROXY_USERNAME=your_webshare_username_here
WEBSHARE_PROXY_PASSWORD=your_webshare_password_here

# Option 2: Generic HTTP Proxy (For Local Development)
# Use with local proxy tools like Clash, V2Ray, etc.
# Format: http://proxy_host:port or http://user:pass@proxy_host:port
# Example: HTTP_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://your_proxy_address:port
# HTTPS_PROXY=http://your_proxy_address:port

# Priority: Webshare proxy is used if configured, otherwise falls back to HTTP_PROXY



